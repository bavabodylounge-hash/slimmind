# 결과지 개인화 완전 설계 로드맵
## "70문항 → P1~P11 완전 매핑 규칙"

**작성일**: 2026-07-15  
**버전**: v1.0  
**목적**: 설문 1~4차(70문항)가 결과지 P1~P11에 순서대로 반영되는 완전한 틀과 규칙  

---

## 📌 핵심 철학: 4단계 내러티브 흐름

```
1차 설문 → "아, 이래야 했구나"     (체형·원인 발견)
2차 설문 → "아, 이런 체질이구나"   (배경·역사 조명)
3차 설문 → "아, 이렇게 해서 실패했구나" (실패 패턴 특정)
4차 설문 → "그래서, 이렇게 해야 하는구나" (처방·행동 결정)
```

이 흐름이 P1→P11 순서에 **그대로 정렬**되어야 한다.

---

## 🔑 커뮤니케이션 축: MBTI × 혈액형 = 말투 결정

### MBTI → 감성형(F) vs 논리형(T) 분류

| 분류 | MBTI 타입 | 말투 원칙 |
|------|-----------|-----------|
| **감성형 (F)** | INFJ, INFP, ENFJ, ENFP, ISFJ, ESFJ, ISFP, ESFP | 감정→사실 순서. "당신이 힘들었던 이유는…" → 팩트 |
| **논리형 (T)** | INTJ, INTP, ENTJ, ENTP, ISTJ, ESTJ, ISTP, ESTP | 사실→해석 순서. "데이터를 보면…" → 의미 |

#### 감성형(F) 문장 패턴
```
[공감 선행] → [사실 서술] → [희망 제시]
예: "매번 다짐하고도 무너졌던 게 의지력이 아니었어요. 
     {오행}의 {특성}이 밤 {시간}에 정확히 {메커니즘}을 만들어서 그랬거든요.
     오늘부터는 그 구조 자체를 바꿉니다."
```

#### 논리형(T) 문장 패턴
```
[팩트 선행] → [원인 분석] → [행동 지시]
예: "{오행} 기질 + {BC코드} 조합에서는 {생리 메커니즘}이 작동합니다.
     {히트맵 데이터}에서 {시간대}에 {증상}이 집중된 것이 이를 뒷받침합니다.
     따라서 처방은 {구체 행동}입니다."
```

### 혈액형 → 행동 경향 보조 힌트 (재미+실용 요소)

| 혈액형 | 다이어트 행동 경향 | 결과지 활용 포인트 |
|--------|-------------------|-------------------|
| **A형** | 계획 집착, 규칙 어기면 자책 | P3에서 "계획 완벽주의가 오히려 위험" 강조 |
| **B형** | 즉흥적, 지루해지면 이탈 | P6 루틴에서 "다양성" 옵션을 먼저 제시 |
| **O형** | 목표 집중, 올인·번아웃 반복 | P4에서 "번아웃 사이클" 특정 트리거로 표현 |
| **AB형** | 냉철-충동 양면, 이론과 실행의 분리 | P3에서 "분석은 많은데 실행이 없는" 패턴 조명 |

> **혈액형은 보조 색채**로만 사용. BC코드·오행·MBTI가 주축.

---

## 🌿 오행 × 운동·식단·회복 → ✅ 이미 구현됨 (건드리지 않음)

> **⚠️ 중요**: 오늘탭 / 12주 로드맵 / 식단·운동·회복 처방은 **이미 코드에 완전히 구현되어 있으므로 이번 개선 범위에서 제외**한다.

**현재 구현 현황:**
- `OHAENG_OVERLAY` (11093줄): 오행별 exercise_add / diet_add / recovery_add / diet_macro_note / tone_caution 정의 완료 ✅
- `getRoadmapWeeks()` (11270줄): BC코드 × 오행 레이어 교차 로직 완성 ✅  
- `SAJU_ELEMENT_DESC[ohaeng].nutriFoods` + `nutriTip`: 오행별 식품군 6대 영양소 완성 ✅
- 오늘탭 식단·운동·회복 트래킹 UI 완성 ✅

**이번 설계 범위: 위 기능들은 전혀 건드리지 않는다.**

---

## 📋 P1~P11 완전 재설계: 페이지별 역할과 데이터 매핑

### 전체 구조 한눈에 보기

```
P1  발견    → "당신의 체형 코드는 이것"       (1차 → BC코드 결론)
P2  원인    → "왜 거기만 찌는지"              (1차+3차 → 증상 원인 설명)
P3  기질    → "당신의 몸 기질"               (1차+2차 → 오행+MBTI+혈액형)
P4  잔혹사  → "왜 매번 실패했는지"           (2차+3차 → 실패 트리거 특정)
P5  위험    → "지금 몸의 적신호"             (2차 → 병력+약물+생활습관)
P6  처방    → "지금 당장 해야 하는 것"       (1차+4차 → 오행×목표 처방)
P7  요약    → "모든 것의 압축"               (전체 통합)
P8  로드맵  → "12주 체중 변화 궤적"          (2차+신체 → 개인 궤적 예측)
P9  미래    → "몸이 어떻게 변하는지"         (2차+신체 → 시뮬레이션)
P10 커뮤니티 → "같은 코드 사람들"            (BC코드 기반)
P11 처방전  → "병원급 맞춤 처방 전문"        (전체 통합 최종 처방)
```

---

### P1: 체형 발견 페이지 (아, 이래야 했구나)

**역할**: "당신이 누구인지" 처음 선언하는 페이지. BC코드가 나오는 순간.

**현재 사용**: bc_code, axis_scores, BC마스터 데이터  
**추가 반영 데이터**:

| 설문 | 문항 | 활용 방법 |
|------|------|----------|
| 1차 | no:1~14 (체형 촉진) | BC코드 결론의 "근거"로 1~2개 문항 언급 |
| 1차 | no:3 (배꼽 아래 촉진) | "복부 질감이 ○○로 느껴지셨죠?" 확인 서술 |
| 1차 | no:5 (탄력 측정) | 셀룰라이트/지방/부종 구분 서술에 반영 |
| 2차 | no:2 (부모 체형) | "부모님 체형 유전 경향과 일치합니다" |

**MBTI/혈액형 적용**:  
- T형: "이 BC코드가 나온 수치 근거: 촉진 ○○, 탄력 ○○, 시간대 분포 ○○"  
- F형: "몸이 오래전부터 이 신호를 보내왔어요. 이제야 제대로 읽혔습니다"

---

### P2: 원인 분석 페이지 (왜 거기만 찌는지)

**역할**: 증상 → 원인축 → "이래서 그랬구나" 연결

**현재 사용**: axis_scores (점수만), BC코드  
**추가 반영 데이터**:

| 설문 | 문항 | 활용 방법 |
|------|------|----------|
| 3차 HM | bloat 시간대 | "붓기가 {시간대}에 집중 → {원인축} 작동 중" |
| 3차 HM | tired 시간대 | "피로가 {시간대}에 집중 → {원인축} 문제" |
| 3차 HM | hungry 시간대 | "식욕 급등이 {시간대} → 혈당/코르티솔 패턴" |
| 3차 HM | pain 시간대 | "결림이 {시간대} → 근감소/골격/림프 중 특정" |
| 3차 HM | sleep 시간대 | "수면 문제가 {시간대} → 호르몬/스트레스" |
| 3차 HM | gut 시간대 | "소화 문제 {시간대} → 장내 환경/인슐린" |
| 2차 | no:5 (손발냉증) | A02(순환)/A03(호르몬) 판별 보조 |
| 4차 | STAGE4_BANK | 경합 축 갈림 결과로 주축 최종 확정 |

**개인화 문장 로직**:
```javascript
// 현재: "A07(스트레스·코르티솔) 축이 높습니다."
// 개선: 
const topSymTime = getTopSymptomTime(stage3Answers); // { tired: '새벽', hungry: '밤' }
// → "새벽에 이유 없이 피곤하고, 밤에 갑자기 당기는 게 반복된다면...
//    그건 코르티솔이 {시간대}에 정확히 작동한 흔적입니다."
```

**MBTI 적용**:
- T형: 히트맵 표 + 수치 + "이 시간대에 이 증상이면 이 축" 명확 설명
- F형: "새벽에 혼자 참았던 그 허기가, 사실 당신 잘못이 아니었어요" → 원인 설명

---

### P3: 기질 분석 페이지 (이런 체질이구나)

**역할**: 오행 × MBTI × 혈액형으로 "나는 이런 사람"을 입체적으로 보여주는 페이지

**현재 사용**: ohaeng_type, mbti, blood_type (재미 요소)  
**추가 반영 데이터**:

| 설문 | 문항 | 활용 방법 |
|------|------|----------|
| 1차 | no:8 (살찐 계기) | 오행 기질과 "이래서 이 계기에 취약" 연결 |
| 2차 | no:8 (최근 계기) | "이 기질 × 이 계기 = 체중 증가 메커니즘" 설명 |
| 2차 | no:11 (출산/군대) | 오행별 "출산 후 왜 이 기질은 특히 힘든지" |
| 2차 | no:1 (가족력) | 오행 유전 경향 ("목 기질은 간 계통 가족력이 많습니다") |

**오행 × MBTI 교차 인사이트 예시**:

| 오행 | MBTI 계열 | 교차 인사이트 |
|------|-----------|--------------|
| 목🌱 | F계열 | "감정 억압이 야식 충동으로 터지는 사이클" |
| 목🌱 | T계열 | "논리적으로 계획 세우지만 감정 처리 루틴 없음" |
| 화🔥 | F계열 | "관계 스트레스가 과부하로 직결" |
| 화🔥 | T계열 | "번아웃까지 모르고 달리다가 갑자기 무너짐" |
| 토🌍 | F계열 | "위로를 음식으로 받는 패턴 고착" |
| 토🌍 | T계열 | "과식인 줄 알면서도 멈추지 못하는 논리적 설명 부재" |
| 금⚡ | F계열 | "한 번 어기면 자책→포기 폭식 루프" |
| 금⚡ | T계열 | "완벽한 계획→한 번 실패→전체 포기 패턴" |
| 수💧 | F계열 | "의욕 없는 기간이 길어져 무기력 고착" |
| 수💧 | T계열 | "데이터는 알지만 실행 에너지가 안 나옴" |

---

### P4: 다이어트 잔혹사 (이렇게 해서 실패했구나)

**역할**: "왜 매번 실패했는지" 그 사람만의 실패 패턴을 정확히 짚어주는 페이지

**현재 사용**: ohaeng_type, mbti, gender, menopause  
**추가 반영 데이터 (핵심)**:

| 설문 | 문항 | 활용 방법 |
|------|------|----------|
| 2차 | no:8 (살찐 계기) | 실패 시작점: "이직·과로" → "코르티솔 폭증 시점부터였군요" |
| 2차 | no:10 (체중 궤적) | 요요 패턴 특정: "올랐다→내렸다→더 올랐다 패턴" |
| 2차 | no:11 (출산/군대) | "출산 후 복벽 약화 → 이 유형은 일반 복근 운동이 역효과" |
| 2차 | no:13 (흡연) | "금연 직후 체중 증가 → 이게 실패처럼 느껴진 것" |
| 2차 | no:14 (음주) | "주 3회 이상 음주 → 간 지방 산화 억제 → 식단 해도 효과 없던 이유" |
| 3차 HM | 전체 | "밤에 특히 식욕이 올라왔던 게 {원인축} 때문이었어요" |
| 3차 HM | hungry 밤/저녁 | "밤 야식 충동이 의지력 문제가 아닌 {호르몬/혈당/코르티솔} 문제" |

**트리거 카드 생성 로직 (개선)**:

```javascript
// 현재: 오행+MBTI 기반 고정 카드
// 개선: 실제 설문 응답 기반 동적 카드

function getCruelHistoryTriggers(ohaeng, mbti, gender, menopause, s2, s3) {
  const triggers = [];
  
  // 2차 no:8 살찐 계기 → 실패 시작점 카드
  if (s2['8'] && s2['8'].includes('이직·과로')) {
    triggers.push({
      icon: '💼', 
      title: '과로 스트레스형 체중 증가',
      desc: '코르티솔이 폭증한 그 시점부터 지방 축적 스위치가 켜졌어요.'
    });
  }
  
  // 2차 no:14 음주 주 3회 이상
  if (s2['14'] === '주 3회 이상') {
    triggers.push({
      icon: '🍺',
      title: '음주 → 지방 산화 억제 사이클',
      desc: '간이 알코올 처리하는 동안 지방 분해가 멈춰요. 식단 해도 효과 없던 이유입니다.'
    });
  }
  
  // 3차 HM: 밤 식욕(hungry)이 강했던 경우
  const nightHunger = s3?.hungry?.[5]; // 밤 시간대 인덱스 5
  if (nightHunger >= 2) {
    triggers.push({
      icon: '🌙',
      title: '밤 폭식은 의지력 문제가 아니에요',
      desc: `${ohaeng} 기질에서 밤에 도파민이 급락하면 식욕 억제가 풀립니다.`
    });
  }
  
  // MBTI 기반 실패 패턴
  const mbtiTrigger = getMbtiFailureTrigger(mbti, ohaeng);
  if (mbtiTrigger) triggers.push(mbtiTrigger);
  
  return triggers;
}
```

**MBTI별 실패 서술 톤**:
- F계열: "그 상황이 너무 힘들었을 거예요. 근데 사실 그건…" → 원인 설명
- T계열: "실패의 원인은 의지력이 아니라 {구체 메커니즘}입니다. 증거: {데이터}"

---

### P5: 위험 신호 페이지 (몸의 적신호)

**역할**: "지금 몸의 상태가 심각한지" 경고 + 안심

**현재 사용**: bc_code, axis_scores  
**추가 반영 데이터**:

| 설문 | 문항 | 활용 방법 |
|------|------|----------|
| 2차 | no:1 (가족력) | "부모님 당뇨 있으면 → 인슐린 저항 위험 높음" |
| 2차 | no:3 (진단질환) | 다낭성/당뇨전단계/갑상선 → BC코드별 주의 메시지 |
| 2차 | no:4 (건강검진) | 공복혈당↑/간수치↑/콜레스테롤↑ → 현재 위험 정도 시각화 |
| 2차 | no:5 (손발냉증) | 수족냉증 심함 → A02/A03 주의 메시지 |
| 2차 | no:6 (장기복용약) | 스테로이드/항우울제/호르몬제 → "이 약이 체중에 이런 영향" |
| 2차 | no:13 (흡연) | 코르티솔 자극 → 복부 지방 축적 위험 강조 |
| 2차 | no:14 (음주) | 간 지방 산화 억제 위험 수준 표시 |

**위험 레벨 계산 로직**:
```javascript
// 위험 플래그 합산
let riskScore = 0;
if (s2['1']?.includes('아버지') || s2['1']?.includes('어머니')) riskScore += 2; // 가족력
if (s2['3']?.includes('당뇨')) riskScore += 3; // 진단 당뇨
if (s2['4']?.includes('공복혈당')) riskScore += 2; // 경계 혈당
if (s2['13'] === '피워요') riskScore += 1; // 흡연
if (s2['14'] === '주 3회 이상') riskScore += 2; // 음주

// riskScore → P5 표시 레벨 결정
// 0~2: "주의 단계" / 3~5: "경고 단계" / 6+: "즉시 개선 단계"
```

---

### P6: 처방 페이지 (지금 당장 해야 하는 것)

**역할**: 오행 × BC코드 교차 처방 — **운동/식단/회복 처방 자체는 이미 완성됨. 건드리지 않음.**

> ✅ **현재 구현 완료**: `getRoadmapWeeks()` + `OHAENG_OVERLAY` + `BC_ROADMAP_DB` 로 BC코드 × 오행 처방이 이미 개인화되어 있음.

**이번 개선 범위 (P6에서 추가할 것)**:

| 설문 | 문항 | 추가 방법 |
|------|------|----------|
| **4차** | desire.moodLabel | P6 **상단 헤더**에 "목표: {moodLabel}한 몸" 한 줄 추가 |
| **4차** | desire.partLabels | P6 헤더에 "집중 부위: {parts}" 태그 추가 |
| **4차** | desire.who | P6 동기 섹션에 "당신이 원하는 방향" 한 줄 반영 |

→ **처방 내용(운동/식단/회복)은 전혀 바꾸지 않고, 페이지 상단 목표 헤더만 개인화**

---

### P7: 종합 요약 페이지

**역할**: P1~P6의 핵심을 한 화면에 압축. "당신의 다이어트 설명서"

**현재 사용**: BC코드, 오행, MBTI, 신체 지표  
**추가 반영 데이터**:

| 추가 항목 | 반영 방법 |
|----------|----------|
| desire.moodLabel | 목표 무드를 "당신이 원하는 모습" 한 줄로 표시 |
| desire.partLabels | "집중 부위" 태그로 표시 |
| 2차 no:8 (계기) | "살찐 계기: ○○" 요약 표시 |
| 4차 STAGE4 결과 | "핵심 원인 축 확정" 표시 |

---

### P8: 로드맵 페이지 (12주 체중 변화 궤적)

**역할**: "나는 12주 동안 어떻게 변하는지" 개인화된 궤적 예측

> ✅ **12주 주차별 로드맵 콘텐츠는 이미 완성됨. 건드리지 않음.**  
> 아래는 **텍스트 서술(내러티브) 개인화만** 추가하는 것.

**이번 개선 범위 (P8에서 추가할 것)**:

| 설문 | 문항 | 추가 방법 |
|------|------|----------|
| 2차 | no:10 (체중 궤적) | P8 **인트로 문장**에 요요 횟수 반영: "이미 N번 겪으셨네요. 이번에 다른 점은…" |
| 2차 | no:11 (출산/군대) | 출산 이력 있으면 → P8 특이사항 배지 "복벽 회복 포함 설계" |
| 4차 | desire.moodLabel | P8 제목 "○○한 몸을 위한 12주" 동적 반영 |

---

### P9: 미래 시뮬레이션 페이지

**역할**: "몸이 어떻게 변하는지" 시각적 미래 예측

> ✅ **시뮬레이션 엔진은 이미 완성됨. 건드리지 않음.**  
> 아래는 **설명 텍스트 개인화만** 추가하는 것.

**이번 개선 범위 (P9에서 추가할 것)**:

| 설문 | 문항 | 추가 방법 |
|------|------|----------|
| 2차 | no:16 (옷사이즈) | P9 설명에 "현재 {size} → 목표 {target_size}" 사이즈 표기 추가 |
| 4차 | desire.partLabels | P9 집중 부위 태그 표시 추가 |

---

### P10: 커뮤니티 페이지

**역할**: 같은 BC코드 사람들과 연결감  
**현재 상태 유지** (BC코드 기반 충분)

---

### P11: 최종 처방전 페이지 (병원급 맞춤 처방)

**역할**: 모든 데이터를 종합한 "처방전" 형식. 가장 구체적이고 전문적인 페이지.

**현재 사용**: bc_code + 5개 _conditions (플래그만)  
**추가 반영 데이터 (전체 활용)**:

| 설문 | 문항 | P11 활용 방법 |
|------|------|-------------|
| 2차 | no:1 (가족력) | "유전 위험도" 섹션에 부모 질환 명시 |
| 2차 | no:2 (부모 체형) | "유전 체형 경향" 참고 |
| 2차 | no:3 (진단질환) | **금기 처방** 결정 (다낭성→당분 제한, 갑상선→요오드 주의) |
| 2차 | no:4 (검진 소견) | **우선 개선 항목** 결정 |
| 2차 | no:5 (손발냉증) | 온열 처방 포함 여부 결정 |
| 2차 | no:6 (복용약) | **약물 상호작용 주의** 섹션 |
| 2차 | no:7 (식욕억제제) | 포만감 신호 민감도 → 처방 조정 |
| 2차 | no:8 (살찐 계기) | "원인 계기" 처방전 상단에 명시 |
| 2차 | no:9 (시술이력) | 시술 부위 고려 처방 (지방흡입 후→림프 케어 강화) |
| 2차 | no:10 (체중 궤적) | 요요 패턴 → 처방 강도 조정 |
| 2차 | no:11 (출산/군대) | 복벽/자세 특이사항 처방 포함 |
| 2차 | no:13 (흡연) | 금연 지원 처방 포함 여부 |
| 2차 | no:14 (음주) | 음주 빈도 → 간 기능 지원 처방 |
| 2차 | no:16 (옷사이즈) | 현재→목표 사이즈 처방 전 명시 |
| **3차** | HM 전체 | **증상별 시간대 처방**: "오후 식욕↑→오전 단백질 보충" |
| **4차** | desire 전체 | 목표 무드·부위 → 처방전 목표 헤더 |
| **4차** | STAGE4 결과 | 최종 원인 축 확정 → 처방 방향 최종 결정 |

**P11 처방전 섹션 구조 (재설계)**:

```
┌─────────────────────────────────────┐
│  🎯 처방 목표                        │
│  "{desire.moodLabel}한 몸"           │
│  집중 부위: {desire.partLabels}      │
├─────────────────────────────────────┤
│  📋 당신의 프로필 요약               │
│  체형코드: {bc_code}                 │
│  오행기질: {ohaeng_type}             │
│  행동성향: {mbti_short}              │
│  살찐계기: {s2.no8}                  │
├─────────────────────────────────────┤
│  ⚠️ 주의사항 (있는 경우만)           │
│  • 진단질환 관련 금기                │
│  • 복용약 상호작용                   │
│  • 시술이력 고려사항                 │
├─────────────────────────────────────┤
│  🍽️ 식단 처방 (오행 × 목표)         │
│  (위 매핑표 기반 구체 식품 리스트)   │
├─────────────────────────────────────┤
│  🏃 운동 처방 (오행 × 목표)          │
│  타이밍: {최고강도 증상 시간대 역}   │
├─────────────────────────────────────┤
│  🔄 회복 처방 (오행 × MBTI)          │
│  (위 매핑표 기반)                    │
├─────────────────────────────────────┤
│  📊 12주 목표                        │
│  현재: {current_size} →              │
│  목표: {desire.partLabels} 개선      │
└─────────────────────────────────────┘
```

---

## 🔧 구현 우선순위 로드맵

> **⚠️ 전제**: 오늘탭 / 12주 로드맵 / 식단·운동·회복 처방 콘텐츠는 **절대 건드리지 않는다.**  
> 개선 대상은 **결과지 텍스트 서술(내러티브)의 개인화**에만 집중한다.

### Phase 1 (즉시, 코드 2~3곳 수정): 데이터 주입

```javascript
// result-hospital.html hospitalInit 함수에서:
window.__RESULT__ = {
  // 기존 유지
  bc_primary, bc_code, user_name, ohaeng_type, mbti,
  axis_scores, gender, _stage2, _bmi, _bmiGrade,
  _conditions, _desire,
  
  // ★ 추가
  _stage3: json.stage3_answers || {},   // 히트맵 원시 응답
  _stage4: json.stage4_answers || {},   // 갈림길 원시 응답
  _desire_full: json.raw_answers?.desire || {}, // 목표 전체
};
```

### Phase 2 (P4 개선): 실패 트리거 카드 동적화

```javascript
// getCruelHistoryTriggers()에 s2, s3 전달
renderP4(userName, ohaengType, mbtiType, answers['Q02'], 
         answers['Q_MENOPAUSE'],
         window.__RESULT__._stage2,   // ★ 추가
         window.__RESULT__._stage3);  // ★ 추가
```

### Phase 3 (P6 개선): 오행 × 목표 처방 교차

```javascript
// desire 데이터 반영
const desire = window.__RESULT__._desire || {};
const moodLabel = desire.moodLabel || '슬림 탄탄한';
const partLabels = desire.partLabels || [];

// 오행 × 목표 처방 로직 실행
const prescription = getOhaengGoalPrescription(ohaengType, moodLabel, partLabels);
```

### Phase 4 (P2 개선): 히트맵 → 원인 서술

```javascript
// stage3 원시 응답으로 "당신의 증상 시간대"를 텍스트로
const s3 = window.__RESULT__._stage3;
const dominantSymptomTime = getDominantSymptomTime(s3);
// → "밤에 식욕이 가장 강했군요" 등 구체 서술
```

### Phase 5 (P5 개선): 2차 병력 반영

```javascript
// no:1,3,4,5,6,13,14 → 위험도 계산 + 주의 메시지
const riskLevel = calculateRiskLevel(window.__RESULT__._stage2);
renderP5Risk(riskLevel, window.__RESULT__._stage2);
```

### Phase 6 (P11 최종 처방전): 전체 통합

```javascript
// 모든 데이터 통합 처방
renderP11({
  ...data,
  _stage2: window.__RESULT__._stage2,
  _stage3: window.__RESULT__._stage3,
  _stage4: window.__RESULT__._stage4,
  _desire: window.__RESULT__._desire_full,
});
```

---

## 📊 데이터 흐름 완전 규칙

```
┌────────────────────────────────────────────────────────────┐
│                    설문 응답 흐름                           │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  1차 (체형 촉진 14문항)                                    │
│    → axis_scores 계산 → BC코드 결정                        │
│    → P1(BC코드 선언) + P2(원인 근거)에 활용                │
│                                                            │
│  2차 (배경/병력 16문항)                                    │
│    → no:6,7,9,12,15 → _conditions 플래그 (현재)           │
│    → no:1,2,3,4,5 → P5 위험도 (★추가)                    │
│    → no:8,10,11 → P4 실패 트리거 + P8 궤적 (★추가)       │
│    → no:13,14 → P5 생활습관 위험 (★추가)                  │
│    → no:16 → P9 사이즈 목표 (★추가)                       │
│                                                            │
│  3차 (히트맵 6증상×6시간대)                               │
│    → axis_scores 보정 (현재)                               │
│    → 원시 응답 → P2 증상 서술 (★추가)                     │
│    → 원시 응답 → P4 실패 트리거 (★추가)                   │
│    → 원시 응답 → P6 처방 타이밍 (★추가)                   │
│    → 원시 응답 → P11 처방전 세부 (★추가)                  │
│                                                            │
│  4차 (갈림길 결정 + desire)                               │
│    → STAGE4_BANK → 경합 축 확정 (현재 일부)               │
│    → desire.moodLabel → P6/P11 처방 목표 (★추가)         │
│    → desire.partLabels → P9/P11 집중 부위 (★추가)        │
│    → desire.who → P7 요약 헤더 (★추가)                   │
│                                                            │
└────────────────────────────────────────────────────────────┘

최종 내러티브 흐름:
P1 → P2 → P3 → P4 → P5 → P6 → P7(요약) → P8 → P9 → P11
"발견" → "원인" → "기질" → "실패이유" → "위험" → "처방" → "압축" → "로드맵" → "미래" → "처방전"
```

---

## 🎯 MBTI × 혈액형 말투 적용 범위

모든 P 페이지의 **핵심 제목 문장**과 **첫 단락**에만 적용 (전체 적용 시 성능 이슈).

```javascript
// 말투 결정 함수
function getToneStyle(mbtiType, bloodType) {
  const isFeeling = ['F'].includes(mbtiType?.[2]); // MBTI 3번째 자리
  
  return {
    isFeeling,
    // F형: 공감 선행, T형: 팩트 선행
    openingStyle: isFeeling ? 'empathy_first' : 'fact_first',
    // 혈액형 보조 색채
    bloodHint: getBloodTypeHint(bloodType, isFeeling),
  };
}

// 각 페이지 제목 문장 생성
function buildPageTitle(page, toneStyle, data) {
  if (toneStyle.isFeeling) {
    // F형: 감정 → 팩트
    return FEELING_TITLES[page](data);
  } else {
    // T형: 팩트 → 해석
    return THINKING_TITLES[page](data);
  }
}
```

---

*이 문서는 코드 구현의 설계 기준입니다.*  
*각 Phase 구현 시 이 문서의 매핑 규칙을 참조하세요.*
